apiVersion: v1
kind: Service
metadata:
  name: pierce
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  ports:
  - name: web-console
    port: 443
    protocol: TCP
    targetPort: 5000
  selector:
    name: director-pierce-frigate
