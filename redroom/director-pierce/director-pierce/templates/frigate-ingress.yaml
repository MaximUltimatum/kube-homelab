apiVersion: v1
kind: Service
metadata:
  name: pierce
  namespace: director-pierce
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  ports:
  - name: web-console
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    name: director-pierce-frigate
