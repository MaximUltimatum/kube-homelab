apiVersion: v1
kind: Service
metadata:
  annotations:
    tailscale.com/tailnet-fqdn: backups-ultron.kingfisher-halfmoon.ts.net
  name: resolve-dammit   # service name
spec:
  externalName: anyvalue   # any value - will be overwritten by operator
  type: ExternalName
