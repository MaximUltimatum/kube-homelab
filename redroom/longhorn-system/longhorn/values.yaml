# Default values for longhorn.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
longhorn:
  persistence:
    defaultClass: true
  env:
  - name: LONGHORN_INSECURE_SKIP_TLS_VERIFY
    value: "true"
