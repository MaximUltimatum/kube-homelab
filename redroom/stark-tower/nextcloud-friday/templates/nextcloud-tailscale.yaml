apiVersion: v1
kind: Service
metadata:
  name: friday
  namespace: stark-tower
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  ports:
  - name: nextcloud
    port: 443
    protocol: TCP
    targetPort: nextcloud
  - name: insecure
    port: 80
    protocol: TCP
    targetPort: insecure
  selector:
    app.kubernetes.io/instance: nextcloud-friday
