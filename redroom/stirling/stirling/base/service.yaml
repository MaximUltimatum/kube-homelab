apiVersion: v1
kind: Service
metadata:
  name: stirling
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app: stirling-pdf
  ports:
  - name: http
    port: 8080
    targetPort: 8080
